[project]
name = "xstate-dsl"
version = "0.1.0"
description = "Bidirectional XState v5 ↔ compact DSL converter"
requires-python = ">=3.10"

[project.scripts]
xstate-dsl = "xstate_dsl.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
